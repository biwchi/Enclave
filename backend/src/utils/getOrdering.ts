import { ProductsOrderig } from 'src/products/constants';

function findOredering(ordering: ProductsOrderig) {
  
}

export const getOredering = (ordering: ProductsOrderig) =>
  findOredering(ordering);
